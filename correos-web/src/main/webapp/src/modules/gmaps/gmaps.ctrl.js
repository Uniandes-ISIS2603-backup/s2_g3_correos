(function(ng)
{
   var mod=ng.module("gmapsModule");
   mod.controller('gmapsCtrl',['$scope', '$http', '$state',
   
          
        function($scope,$http,zonasContext,$state)
        {
           
            
        }
        
   ]);
}
)(window.angular);